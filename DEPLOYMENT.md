# ありか：会社・組織ごとの導入手順

## 配布方式

1社につき独立したSiteを設置します。各Siteで画像保存先（R2）、データベース（D1）、管理用トークン、招待コード、OpenAI APIキー、累計利用額を分離します。共有サービス内で複数社を管理する方式ではありません。

現在稼働中のSiteを更新する場合は、既存の `.openai/hosting.json`、保存先、実行時設定を維持します。下記の新規設置手順を既存Siteに適用しないでください。管理用トークンの変更は既存の管理リンクとAPIキーの復号に影響します。

## 新しい会社への設置

1. 最新ソースをGitHubから取得します。配布元で `node scripts/export-installation.mjs /absolute/path/arika-new-company` を実行してください。出力先は存在しない、元リポジトリ外のディレクトリを指定します。この処理はコミット済みソースだけを出力し、元SiteのIDを除去します。画像・DB・キー・招待リンク・Git履歴・ビルド成果物はコピーしません。
2. 設置先企業のChatGPTアカウント／ワークスペースを選択し、Sitesで上記ディレクトリから**新しいSite**を作成します。作成・公開権限とSitesの利用可否は設置先で確認してください。元のSite IDを流用せず、返された新しいIDを使用します。元のGitHubリポジトリを参照する場合も、元の `.openai/hosting.json` にあるIDで公開してはいけません。
3. Sitesの手順に従い、このSite専用のD1 `DB` とR2 `BUCKET` を用意し、既存のDrizzleマイグレーションを適用します。別企業や配布元の実際のDB・バケットを指定しません。依存関係の導入・ビルド・公開はSitesの現在のワークフローに従います。
4. この会社専用の `ADMIN_TOKEN` を暗号学的乱数32バイト以上から生成し、Sitesの秘密の実行時設定に登録します。生成例：`node -e "console.log(require('node:crypto').randomBytes(32).toString('hex'))"`。ソースや公開文書へ記載しません。最初にアクセスした人が管理者になる方式ではありません。
5. `OPENAI_API_KEY` は未設定のままにします。元の会社の実行時設定やDBを複製しないでください。`AI_BUDGET_CAP` はこの会社で許可する累計AI利用額の最大値です。未設定なら **$4.50**、0なら停止。例：総予算$5に対して$0.50を予備とするなら4.50に設定します。月次リセットはありません。
6. 希望する公開範囲で公開します。URLは新しいSiteの公開結果から取得します。特定の企業URLをアプリに入力するだけで公開先は変わりません。企業ワークスペースでは管理者が公開範囲を制限する場合があります。
7. 新しいSiteのURLに `/#manage=＜その会社のADMIN_TOKEN＞` を付けて管理者が開きます。認証済みの初回アクセス時だけ空の共有スペースを作ります。既存スペース・招待コードは初期化しません。
8. 「共有・設定」で組織名とAI利用上限を設定します。APIキーは空欄のままで、撮影画像の保存・閲覧・手動メモ検索を開始できます。
9. AIが必要になったら同画面で各社のOpenAI APIキーを登録します。API呼び出しの費用はそのキーの契約先に発生します。キーは暗号化して保存し、ブラウザに読み戻しません。空欄のまま組織設定を保存しても登録済みのキーは消しません。実行時 `OPENAI_API_KEY` が設定されている場合はそちらを優先します。
10. 「共有・設定」から**招待リンク**をコピーして利用者に渡します。管理用リンクは管理者だけが保管します。公開範囲が限定されているSiteでは、Sites側の閲覧許可も必要です。

## 利用上限の動作

画面のAI利用上限は0から `AI_BUDGET_CAP` まで、米ドル・小数第2位まで設定できます。変更しても既存の累計利用額をリセットしません。利用額より低く設定した場合、新しいAI処理は停止し、保存・閲覧・文字検索は継続できます。

API送信前に画像3枚ごとに$0.05を仮確保します。並行操作でも仮確保が上限を超えないようDBで判定します。実際の料金は概算であり、予算変更前に送信済みの処理は完了・計上されます。OpenAI側の他の利用はこのSiteの集計に含まれません。現在の1回分仮確保方式を維持しており、請求額の厳密な保証ではありません。

## 導入確認

- 初回管理アクセス後、API未設定・画像0件・利用額0で始まる。
- 管理用リンクを再度開いても招待コードが変わらない。
- API未設定で撮影とメモを保存し、メモから検索できる。
- 招待利用者は画像を閲覧・検索できるが、組織設定・キー・画像削除を操作できない。
- 許可したAPIキーを登録してから、少数の画像でAI解析を確認する（有料）。
- 異なる会社の管理リンク・招待コードではアクセスできず、データが混ざらない。

## 現在の制約

管理権限は管理用リンクを持つ人に与えます。ChatGPTアカウントによる役職判定・管理者の複数ユーザー管理・企業SSOは未実装です。公開・権限設定はSitesで管理し、アプリ内の招待認証と併用します。企業の実環境への設置とiPhone Safari実機確認は各設置後に実施してください。
